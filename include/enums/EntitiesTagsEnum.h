#ifndef ENTITIES_TAGS_ENUM_H
#define ENTITIES_TAGS_ENUM_H

/**
 * @brief Enums contains codes of tags as const integers.
 * 
 */
enum EntitiesTagsEnum {
    EMPTY, FOOD
};

#endif // ENTITIES_TAGS_ENUM_H